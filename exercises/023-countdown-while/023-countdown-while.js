export function countdownWhile() {
  // TODO: Create a countdown from 10 to 1 using a while loop
  //
  // A while loop repeats code as long as a condition is true:
  //
  // while (condition) {
  //   // code to repeat
  // }
  //
  // Steps to create a countdown:
  // 1. Create a variable starting at 10
  // 2. Use a while loop that continues while the variable is >= 1
  // 3. Inside the loop, log the current number
  // 4. Decrease the number by 1
  // 5. After the loop ends, log "Blast off!"
  //
  // Hint: Use console.log() to display each number
  // Hint: Use count-- or count = count - 1 to decrease the number



  // let count = 10;

  /* TODO: Your code goes here */

  // console.log("Blast off!");
}

// Run the countdown
console.log("=== Countdown Timer ===\n");
countdownWhile();

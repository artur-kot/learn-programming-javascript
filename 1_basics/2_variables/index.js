/*
Exercise 2: Store your name in a variable and print it.

Instructions:
- Create a variable called `myName` and assign it your name as a string.
- Print the value of `myName` using console.log.

Tip: You can change this file and run `node ./index.js` from inside the `2_variables/` folder.
*/

// TODO: Replace the value with your own name.
const myName = 'Ada Lovelace';

console.log(myName);
